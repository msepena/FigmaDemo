import CoreGraphics

public enum DSRadius {
    public static let cell: CGFloat = 16
    public static let card16: CGFloat = 16
    public static let card24: CGFloat = 24
    /// Used with `Capsule()` or large value (effectively pill)
    public static let pill: CGFloat = 100
}
